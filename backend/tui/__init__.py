"""Flatland TUI — a terminal client for the world simulation.

Watches/controls the live sim over the existing /ws + REST API.
Pure client: never imports backend code (only mirrors its protocol).
"""

__version__ = "0.1.6"
